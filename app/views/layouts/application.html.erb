<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= content_for(:title) || "Marketplace V1" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Marketplace V1">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <!-- PWA manifest (optional) -->
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-screen bg-blue-50  text-gray-900 font-sans">

    <div class="flex flex-col min-h-screen">

      <!-- Navigation -->
  <header class="bg-gray-900 rounded-full text-white shadow-md">
    <div class="container mx-auto flex items-center justify-between px-6 py-4">

      <!-- Logo & Main Nav -->
      <div class="flex items-center space-x-6">
        <%= link_to root_path, class: "flex items-center space-x-2" do %>
          <img src="/icon.png" alt="Marketplace Logo" class="w-10 h-10 rounded-full shadow-lg">
          <span class="text-2xl font-bold text-white hover:text-blue-400 transition-colors">Marketplace</span>
        <% end %>

        <% if authenticated? %>
          <nav class="hidden md:flex space-x-4">
            <%= link_to "Dashboard", root_path, class: "text-gray-200 hover:text-white font-medium transition-colors" %>
            <%= link_to "Wishlists", wishlists_path, class: "text-gray-200 hover:text-white font-medium transition-colors" %>
            <%= link_to "Settings", settings_root_path, class: "text-gray-200 hover:text-white font-medium transition-colors" %>
          </nav>
        <% end %>
      </div>

      <!-- User Actions / Profile -->
      <div class="flex items-center space-x-4">

        <% if authenticated? %>
          <!-- Optional: Notifications Icon -->
          <button class="relative p-2 rounded-full hover:bg-gray-800 transition-colors">
            <svg class="w-6 h-6 text-gray-200" fill="none" stroke="currentColor" stroke-width="2"
                viewBox="0 0 24 24" stroke-linecap="round" stroke-linejoin="round">
              <path d="M18 8a6 6 0 0 0-12 0c0 7-3 9-3 9h18s-3-2-3-9"></path>
              <path d="M13.73 21a2 2 0 0 1-3.46 0"></path>
            </svg>
            <!-- Notification badge -->
            <span class="absolute top-0 right-0 inline-block w-2 h-2 bg-red-500 rounded-full"></span>
          </button>

          <!-- Profile Dropdown -->
          <div class="relative group">
            <button class="flex items-center space-x-2 p-2 rounded hover:bg-gray-800 transition-colors">
              <span></span>
              <img src="/icon.png" alt="Avatar" class="w-8 h-8 rounded-full">
            </button>

            <!-- Dropdown menu -->
            <div class="absolute right-0 mt-2 w-48 bg-gray-800 rounded-lg shadow-lg opacity-0 invisible group-hover:visible group-hover:opacity-100 transition-all duration-200 z-50">
              <%= link_to "Profile", settings_profile_path, class: "block px-4 py-2 text-gray-200 hover:bg-gray-700 rounded-t-lg transition-colors" %>
              <%= link_to "Account", settings_user_path, class: "block px-4 py-2 text-gray-200 hover:bg-gray-700 transition-colors" %>
              <%= button_to "Log out", session_path, method: :delete,
                    class: "w-full text-left px-4 py-2 text-gray-200 hover:bg-gray-700 rounded-b-lg transition-colors" %>
            </div>
          </div>

        <% else %>
          <%= link_to "Sign Up", sign_up_path, class: "bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 transition-colors font-medium" %>
          <%= link_to "Login", new_session_path, class: "text-gray-200 hover:text-white font-medium transition-colors" %>
        <% end %>
      </div>

      <!-- Mobile Menu Button -->
      <div class="md:hidden">
        <button class="p-2 rounded hover:bg-gray-800 transition-colors">
          <svg class="w-6 h-6" fill="none" stroke="currentColor" stroke-width="2"
              viewBox="0 0 24 24" stroke-linecap="round" stroke-linejoin="round">
            <path d="M4 6h16M4 12h16M4 18h16"></path>
          </svg>
        </button>
      </div>

    </div>
  </header>


      <!-- Flash Messages -->
      <div class="container mx-auto px-6 mt-6">
        <% if flash[:notice] %>
          <div class="bg-green-100 text-green-800 border border-green-300 rounded-md p-4 mb-4">
            <%= flash[:notice] %>
          </div>
        <% end %>
        <% if flash[:alert] %>
          <div class="bg-red-100 text-red-800 border border-red-300 rounded-md p-4 mb-4">
            <%= flash[:alert] %>
          </div>
        <% end %>
      </div>

      <!-- Main Content -->
      <main class="flex-grow container mx-auto px-6 pb-12">
        <div class="bg-white shadow-md rounded-lg p-6">
          <%= content_for?(:content) ? yield(:content) : yield %>
        </div>
      </main>

      <!-- Footer -->
      <footer class="bg-gray-100 border-t mt-auto">
        <div class="container mx-auto px-6 py-4 text-center text-gray-500 text-sm">
          &copy; <%= Time.current.year %> Marketplace V1. All rights reserved.
        </div>
      </footer>

    </div>
  </body>
</html>
