<div class="max-w-4xl mx-auto space-y-6">

  <!-- Page Title -->
  <h1 class="text-3xl font-bold text-gray-900">Your Wishlists</h1>

  <!-- Create New Wishlist Button -->
  <div>
    <%= link_to "Create a Wishlist", new_wishlist_path, class: "inline-block px-4 py-2 bg-blue-500 text-white rounded hover:bg-blue-600" %>
  </div>

  <!-- Wishlists List -->
  <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
    <%= render @wishlists %>
  </div>

</div>
